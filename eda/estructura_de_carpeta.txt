Documentos en carpeta eda:

- En la carpeta scripts están los scripts (.ipynb) nombrados con un breve título descriptivo y con el prefijo codigo_dia_x_ (con x de 1 a 5). También se incluye el archivo “eda_reporting_ecommerce_renderizado_para_jupyterNb.ipynb” que corresponde el archivo original de “codigo_dia_5_eda_reporting_ecommerce.ipynb” que renderiza bien en Jupyter notebook, pero mal en GitHub.

- En la carpeta outputs está la subcarpeta outputs_dia_x (con x={3, 4}) en la que se almacenan las salidas de la ejecución del script correspondiente al día x, como por ejemplo, imágenes de gráficos.

- "estructura_de_carpeta.txt" es este documento.
